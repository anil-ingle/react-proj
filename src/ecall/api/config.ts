/** all the static data required for app to run */
const config = {
    protocol: 'http',
    host: 'http://localhost:2020/onlineparking/',
    locale: '',
    language: 'JS'
};

export default config;