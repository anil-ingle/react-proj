import { Button, Input, Modal } from 'antd';
import styled from 'styled-components';

export const CancelButton = styled(Button as any)`
    background:red;
    type="default"
    color:white;
`;
export const SnedButton = styled(Button as any)`
    background:green;
    color:white;
    type="default"
`;
export const ModalForgetPassword = styled(Modal as any)`
    text-align:center;
    top:33%;
    width:550px;
`;
