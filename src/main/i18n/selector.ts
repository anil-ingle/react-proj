import { createSelector } from 'reselect';
// //import { EComStore } from '../../ecall/types';

// /**
//  * Direct selector to the languageToggle state domain
//  */
// const selectLanguage = (store: EComStore) => store.get('language');

// /**
//  * Select the language locale
//  */
// const makeSelectLocale = () => createSelector(
//   selectLanguage,
//   (languageState): string => languageState.get('locale')
// );

// export {
//   selectLanguage,
//   makeSelectLocale,
// };
